{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "跑预览插件(扩展开发宿主)",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/vscode-plot-preview",
        "${workspaceFolder}"
      ]
    }
  ]
}
